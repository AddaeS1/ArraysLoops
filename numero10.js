const numeros = [54, 22, 14, 8, 284];


for (let numero of numeros) {
    if (numero === 10) {
        console.log(numeros.indexOf(numero));
            }else{
                console.log(-1)
                 }
                break;
               }
